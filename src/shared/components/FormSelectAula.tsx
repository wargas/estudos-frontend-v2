import { useState } from 'react'
import { DrawerComponentProps } from '.'

const SelectAula = ({ data, setWidth }: DrawerComponentProps) => {

    const [search, setSearch] = useState()

    return <div>
        SElec
    </div>
}

export default SelectAula